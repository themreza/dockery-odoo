;; ==================================
;; ========== CUSTOM SECTIONS =======
;; ==================================
[backups]
backupfolder = /var/lib/odoo-bkp
[custom]
;; You also can define your own section name
;; Use in code like this...
;; import tools
;; sect = 'custom'
;; conf_value = tools.config.get_misc(sect, key, default=None)
